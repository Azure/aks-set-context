{
   "name": "aks-set-context-action",
   "version": "5.0.0",
   "private": true,
   "type": "module",
   "main": "lib/index.js",
   "scripts": {
      "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=lib/index.js --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\"",
      "test:integration": "node scripts/integration-test.mjs",
      "test": "vitest run",
      "test-coverage": "vitest run --coverage",
      "format": "prettier --write .",
      "format-check": "prettier --check .",
      "prepare": "husky"
   },
   "keywords": [
      "actions",
      "node",
      "setup"
   ],
   "author": "GitHub",
   "license": "MIT",
   "dependencies": {
      "@actions/core": "^3.0.1",
      "@actions/exec": "^3.0.0",
      "@actions/io": "^3.0.2",
      "husky": "^9.1.7"
   },
   "devDependencies": {
      "@types/node": "^25.6.0",
      "@vitest/coverage-v8": "^4.1.5",
      "esbuild": "^0.28.0",
      "prettier": "^3.8.3",
      "typescript": "^6.0.3",
      "vitest": "^4.0.18"
   }
}
